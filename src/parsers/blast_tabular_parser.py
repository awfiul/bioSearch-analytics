"""BLAST tabular parser placeholder."""

from __future__ import annotations

import pandas as pd


def parse_blast_tabular(file) -> pd.DataFrame:
    """Parse BLAST tabular output into the unified DataFrame schema."""
    raise NotImplementedError("BLAST tabular parsing will be implemented in a later stage.")
