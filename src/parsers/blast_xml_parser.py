"""BLAST XML parser placeholder."""

from __future__ import annotations

import pandas as pd


def parse_blast_xml(file) -> pd.DataFrame:
    """Parse BLAST XML into the unified DataFrame schema."""
    raise NotImplementedError("BLAST XML parsing will be implemented in a later stage.")
