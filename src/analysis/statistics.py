"""Summary statistics placeholder."""

from __future__ import annotations

import pandas as pd


def calculate_summary(df: pd.DataFrame) -> dict:
    """Calculate summary metrics for BLAST hits."""
    raise NotImplementedError("Summary statistics will be implemented in a later stage.")
